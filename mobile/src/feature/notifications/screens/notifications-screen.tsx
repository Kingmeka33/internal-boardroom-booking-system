import { PlaceholderScreen } from "@shared/components/placeholder/placeholder-screen";

export function NotificationsScreen() {
  return (
    <PlaceholderScreen
      eyebrow="Updates"
      title="Notifications"
      description="Notification delivery and read states will build on the notification state foundation."
    />
  );
}
